# ============================================================
# P3 · LangGraph 入门 阶段依赖
# ============================================================
# 说明：langgraph 已随 langchain 1.3.x 自动安装（项目 .venv 里已有 1.2.10），
# 这里显式声明只是为了"清单完整、换机器可复现"。这是轻量纯 Python 包。
#
# 安装命令（如有需要）：
#   ~/Workbuddy/langchain-langGraph/.venv/bin/python -m pip install -r requirements.txt
langgraph>=1.2.0
