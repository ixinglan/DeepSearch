# ============================================================
# P2 · Tools & Agents 阶段依赖
# ============================================================
# 说明：P0/P1 已装好的基础包不用重装（langchain、langchain-openai、pydantic、python-dotenv）
# 这里只列 P2 新增的依赖。安装命令：
#   ~/Workbuddy/langchain-langGraph/.venv/bin/python -m pip install -r requirements.txt
#
# langgraph：ReAct Agent 的运行时（create_react_agent 在 langgraph.prebuilt 里）
#   注意：langchain 1.3.x 本身已经依赖 langgraph（会随 langchain 自动装上），
#   这里显式声明是为了"清单完整、换机器可复现"。这是轻量纯 Python 包，不是大包。
langgraph>=1.2.0
